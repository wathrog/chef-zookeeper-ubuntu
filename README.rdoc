= DESCRIPTION:
Chef recipe to install Zookeeper client or server.

* Server scope: environment.
* Client scope: the target machine, the config files are generated by passing as parameter the environment where the servers resides.

= REQUIREMENTS:

- Ubuntu (tested on 11.04 Natty)

= ATTRIBUTES:

TBD

= USAGE:

TBD

